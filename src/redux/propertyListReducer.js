const defaultState = {
    fetchedProperties: []
}

const streamListReducer = (state = defaultState, action) => {
    // switch (action.type)
    return {
        ...state
    }    
}

export default streamListReducer;